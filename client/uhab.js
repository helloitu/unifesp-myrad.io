malarkey(document.querySelector('.rotating'), {
    typeSpeed: 50,
    deleteSpeed: 50,
    pauseDelay: 2000,
    loop: true,
    postfix: ''
  })
  .type('Guilherme').pause().delete()
  .type('Arthur').pause().delete()
  .type('Abner').pause().delete()
  .type('Felipe').pause().delete()
  .type('Italo').pause().delete()
  .type('Jhonson').pause().delete()
